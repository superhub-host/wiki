---
title: Изменение версии Minecraft сервера
description: Инструкция по автоматической и ручной смене версии или ядра Minecraft сервера.
---

Версию Minecraft можно изменить без удаления услуги. Если выбранная при покупке версия тебе не подошла или сервер не запускается, не спеши его перезаказывать, иначе можно потерять файлы, мир, настройки и уже оплаченный период. Сначала попробуй автоматическую или ручную установку. Новый же сервер нужен только в редких случаях, например, если ты хочешь перейти на другой тип услуги, допустим, с Minecraft на VPS.

!> Перед любыми действиями лучше сделать резервную копию. Новые версии Minecraft умеют обновлять миры, но старые почти никогда не могут безопасно открыть мир, который уже запускался на более новой версии.

## Установка при покупке

При заказе Minecraft сервера ты выбираешь версию из самых популярных вариантов, и хостинг автоматически устанавливает её. После установки можно открыть панель, запустить сервер и подключиться к нему по адресу из личного кабинета. Если сборке нужна конкретная версия [Forge](https://files.minecraftforge.net/net/minecraftforge/forge/), проверь параметры установщика перед переустановкой — они описаны ниже.

## Автоматическая смена

Самый простой способ изменить версию или ядро — использовать встроенный установщик:

1. Открой [личный кабинет](https://my.superhub.host) и выбери нужную услугу.
2. Полностью останови сервер кнопкой «Стоп» во вкладке «Консоль».
3. Для смены версии открой «Настройки», укажи нужную версию в доступных полях установщика и переустанови сервер по красной кнопке внизу. Поле точной версии Forge необязательно — правила его заполнения описаны ниже.
4. Для смены ядра нажми фиолетовую кнопку в самом низу «Настроек».
5. Проверь файлы и команду запуска. Если ядро запускается через `-jar`, в поле «Исполняемый файл» должно быть точное имя загруженного `.jar`-файла, например `server.jar` или `forge-26.1.2-64.0.9-shim.jar`. Если Forge или NeoForge запускается через `@unix_args.txt`, отдельного `server.jar` может не быть: проверь наличие файла аргументов по пути из команды и папки `libraries`. Подробнее — в [статье о команде запуска](/guides/hosting/change-startup-command#forge-и-unix_argstxt).
6. Включи сервер и проверь консоль. В случае понижения версии может понадобиться удалить старую папку мира (обычно это «world»).

?> При смене ядра в личном кабинете файлы сервера сохраняются. Обычно перезаписываются сам `.jar`-файл ядра и некоторые файлы в `libraries`. На всякий случай заранее сделай резервную копию мира, конфигураций и других важных файлов.

![Поля версии Minecraft, типа сборки и версии Forge](/images/guides/minecraft/change-version/version-settings.png)

На скриншоте показаны переменные во вкладке «Настройки»: версия Minecraft, тип сборки Forge и точная версия Forge. Если поле «Версия Forge» пустое, используются версия Minecraft и тип сборки. Если указать точную версию Forge, например `1.15.2-31.2.4`, значения полей «Тип сборки» и «Версия Minecraft» игнорируются. Выбери подходящий вариант вместо того, чтобы обязательно заполнять все поля.

?> В Pterodactyl во вкладке «Запуск» некоторые переменные могут быть доступны только для чтения. Их наличие на странице не означает, что пользователь может изменить их здесь. Для изменения параметров установщика используй личный кабинет; если нужное поле недоступно и там, обратись в [поддержку](/support).

## Ручная смена

Ручная установка нужна, если в панели нет нужного билда или требуется редкая версия. Для ядер, которые распространяются готовым `.jar`-файлом, порядок такой; для Forge и NeoForge смотри [раздел ниже](#forge-и-neoforge).

1. Останови сервер во вкладке «Консоль» панели управления сервером.
2. Скачай нужное ядро только с официального сайта проекта (ссылки для скачивания ниже).
3. Открой вкладку «Файлы» или подключись к серверу через [SFTP](/guides/use-sftp).
4. Загрузи новый `.jar`-файл в корневую папку сервера.
5. Укажи название загруженного файла во вкладке «Настройки» сервера в личном кабинете.

![Поле «Исполняемый файл» в настройках сервера](/images/guides/minecraft/change-version/jar-file-setting.png)

В поле «Исполняемый файл» должно быть указано точное название `.jar`-файла, который лежит в корне сервера. Если файл называется не `server.jar`, впиши его реальное имя.

## Ссылки для скачивания

Скачивай ядра только с официальных сайтов. Файлы из случайных Telegram-каналов, архивов и «зеркал» могут содержать вредоносный код или устаревшие сборки.

| Ядро | Где скачать | Когда использовать |
| --- | --- | --- |
| Vanilla Java Edition | [minecraft.net](https://www.minecraft.net/en-us/download/server) | Чистый сервер без плагинов и модов. |
| Paper | [papermc.io](https://papermc.io/downloads/paper) | Оптимизированный сервер с плагинами Bukkit, Spigot и Paper. |
| Purpur | [purpurmc.org](https://purpurmc.org/download/purpur) | Форк Paper с дополнительными настройками геймплея. |
| Fabric | [fabricmc.net](https://fabricmc.net/use/server/) | Сервер с Fabric-модами. |
| Forge | [files.minecraftforge.net](https://files.minecraftforge.net/) | Сервер со старыми и популярными Forge-модпаками. |
| NeoForge | [neoforged.net](https://neoforged.net/) | Современные модпаки, которые перешли с Forge на NeoForge. |
| Velocity | [papermc.io](https://papermc.io/downloads/velocity) | Прокси для сети серверов Minecraft: Java Edition. |
| BungeeCord | [ci.md-5.net](https://ci.md-5.net/job/BungeeCord/) | Старый прокси для сети Java Edition серверов. |
| Vanilla Bedrock Edition | [minecraft.net](https://www.minecraft.net/en-us/download/server/bedrock) | Официальный сервер Minecraft: Bedrock Edition. |
| PocketMine-MP | [pmmp.io](https://pmmp.io/) | Сервер Bedrock Edition с плагинами PocketMine. |
| Cloudburst Nukkit | [cloudburstmc.org](https://cloudburstmc.org/) | Java-ядро для Minecraft: Bedrock Edition. |

?> Переход между Java Edition и Bedrock Edition не всегда возможен простой заменой `.jar`-файла: не все Bedrock-ядра работают на Java. Например, Nukkit использует Java, а официальный Bedrock-сервер и PocketMine-MP — нет. Кроме того, для Bedrock на хостинге действуют другие тарифы. Для перехода напиши в [техническую поддержку](/support), чтобы подобрать тариф и настроить запуск нужного ядра.

## Forge и NeoForge

Forge и NeoForge устанавливаются не так, как Paper или Purpur. Обычно у них есть инсталлятор, папка `libraries`, дополнительные файлы запуска и свои требования к версии Java. Если просто заменить `server.jar` на файл инсталлятора, сервер может не запуститься. Для таких случаев используй [отдельную инструкцию](/guides/install-forge).

## Версия Java

Разным версиям Minecraft нужна разная Java. Если версия Java не подходит, сервер может завершиться сразу после запуска.

Частый пример ошибки:

```text
java.lang.UnsupportedClassVersionError: net/minecraft/server/Main has been compiled by a more recent version of the Java Runtime
```

Эта ошибка означает, что ядро собрано под более новую Java, чем та, которая выбрана на сервере. Бывает и наоборот: старые Forge-сборки, например на Minecraft 1.12.2 или 1.7.10, часто требуют Java 8 и могут не запускаться на Java 17 или Java 21.

Изменить Java можно по инструкции [«Выбор и смена версии Java»](/guides/minecraft/java-version).

## Распространённые ошибки

### Сервер не видит файл

В консоли появляется ошибка:

```text
Error: Unable to access jarfile server.jar
```

Команда запуска ищет файл `server.jar`, но такого файла нет в корневой папке. Проверь имя файла, расширение `.jar` и регистр букв. Для Linux имена `server.jar` и `Server.jar` считаются разными. Можно переименовать загруженное ядро в `server.jar` или указать его реальное имя в поле «Исполняемый файл» — смотри [настройку команды запуска](/guides/hosting/change-startup-command#файл-запуска).

Если твоя сборка Forge или NeoForge использует `unix_args.txt`, замена имени `.jar` не решит проблему. Проверь, что команда запуска читает файл через `@`, путь к нему соответствует установленной версии и папка `libraries` загружена целиком. Подробности и пример команды — в [разделе про unix_args.txt](/guides/hosting/change-startup-command#forge-и-unix_argstxt).

### Мир от новой версии

После отката версии в логах может появиться сообщение:

```text
[17:38:24 WARN]: java.lang.RuntimeException: Server attempted to load chunk saved with newer version of minecraft! 2586 > 1343
Server attempted to load chunk saved with newer version of minecraft
```

Это значит, что сервер пытается открыть мир, который уже сохранялся на более новой версии Minecraft. Надёжное решение — вернуть прежнюю версию сервера, восстановить бэкап или удалить папки мира (`world`, `world_nether`, `world_the_end`) и сгенерировать карту заново.

### Не подходят моды

Если сервер запускается, но падает при загрузке модов, проверь версии модов, загрузчика и Minecraft. Например, Fabric-мод для `1.20.1` не обязан работать на `1.20.4`, а Forge-мод нельзя просто перенести на NeoForge без проверки совместимости.

### Не работает команда version

На Paper, Purpur, Spigot и похожих ядрах можно проверить версию командой `version` в консоли. На Vanilla, Forge и Fabric такой команды может не быть. В этом случае смотри первые строки запуска в консоли или файл `logs/latest.log`.
